# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Firebase
.firebase/
firebase-debug.log*
firestore-debug.log*

# Environment variables
.env
.env.local
.env.*.local

# Editor directories and files
.idea
.vscode/*
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# OS
Thumbs.db

# Local build / deploy state (never commit)
.next/
*.tsbuildinfo
.vercel/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# Evaluation run output (input data test_data.jsonl stays tracked)
evaluation/evaluation_results.json

# Large media — hosted externally, not in git
public/assets/*.mp4
public/assets/*.MP4

# macOS resource forks that slipped upstream (._* files)
._*

# Next build cache that slipped upstream
*.sst
